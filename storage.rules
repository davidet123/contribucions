rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Carpeta d'imatges: accés lliure per ara.
    // Quan afegiu autenticació, canvieu per:
    //   allow read, write: if request.auth != null;

    match /imatges/{imageId} {
      allow read, write: if true;
    }
  }
}
